apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "apartments.fullname" . }}-test-connection"
  labels:
    {{- include "apartments.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "apartments.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
