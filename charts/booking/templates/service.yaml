apiVersion: v1
kind: Service
metadata:
  name: {{ include "apartments.fullname" . }}
  labels:
    {{- include "apartments.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "apartments.selectorLabels" . | nindent 4 }}
